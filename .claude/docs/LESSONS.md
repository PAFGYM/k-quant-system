# K-Quant 교훈 (Lessons Learned)

## 환경 설정
- .env load_dotenv(override=True) 필수
  → Claude Code가 빈 ANTHROPIC_API_KEY를 시스템 환경변수에 설정하기 때문

## AI 컨텍스트
- build_full_context_with_macro(db, macro_client) 사용 필수
  → macro_snapshot 누락 시 시장 데이터 "없음"으로 나옴
- 실시간 주가는 yfinance에서 조회 후 컨텍스트에 주입

## UX / 메시지
- 모바일 가독성: 짧은 문장(25자), 줄바꿈, 이모지 구분
- 관심/매수/매도 포인트: 🟡🟢🔴 으로 구분
- 자연어 입력 우선 ("삼성전자 분석해줘"), 슬래시 명령 보조

## 포트폴리오 관리
- 자연어 매수 감지: "삼성전자 50주 76000원" → 확인 버튼
- 스크린샷 신규 종목 → "포트폴리오에 추가해드릴까요?" 제안
- DB active_holdings에 없는 종목만 제안

## 코드 패턴
- get_latest_screenshot = get_last_screenshot 별칭 존재
- sqlite.py: get_job_runs(run_date) 메서드 사용

## Phase 히스토리
- Phase 10: 자연어 입력 + 포트폴리오 자동 관리
- v3.5: 매수 플래너, 실시간 급등 감지, 매도 가이드
- v3.6: 백테스트 프로 (TradeCosts, 포트폴리오 백테스트)
- v3.7: 리스크 엔진 (VaR, Monte Carlo, Stress Test) + LSTM 예측
- v3.8: E2E 안정화, 고급 리스크 콜백, LSTM 재학습 잡
